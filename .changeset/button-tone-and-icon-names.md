---
'design-system-icons': minor
---

Add consumer-friendly Button APIs: `label` for straightforward text, semantic `color` presets for success/warning/error styling without inline style objects, and direct override props (`surfaceColor`, `borderColor`, `textColor`, `accentColor`) for theme-token-driven customization. Also add React `leadingIconName`/`trailingIconName` convenience props that render the shared Icon component with `currentColor`.
